- name: install applications
  hosts: localhost
  become: yes 
  tasks:
  - name: install packages
    ansible.builtin.debug:
  
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql

