- name: install applications
  hosts: localhost
  become: yes 
  tasks:
  - name: install packages
    ansible.builtin.debug:
     yum: name= "{{ item }}" state= latest
    loop:
     - nginx
     - mysql  

