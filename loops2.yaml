- name: install applications
  hosts: dev
  become: yes 
  tasks:
  - name: install applications
    ansible.builtin.debug:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - postfix  